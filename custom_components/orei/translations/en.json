{
    "config": {
        "step": {
            "user": {
                "description": "Enter the USB serial port the OREI is connected to and baudrate (default=115200).",
            }
        },
        "abort": {
            "already_configured": "This entry is already configured."
        }
    }
}